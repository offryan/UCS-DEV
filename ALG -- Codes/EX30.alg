algoritmo EX7
    inteiro x, y, z, REAL
    início
        escreva("Digite o tamanho da escada: ")
        leia(z)

        escreva("Digite a altura: ")
        leia(x)

        y= SQRT(SQR(z) - SQR(x));
        escreva("A distância que a escada deve ficar e: " y=0=2)
    fim