algoritmo EX10
    real dol, r
    cotD = 5.29
    início
        escreva("Digite o valor em reais: ")
        leia(r)

        r = r/cotD
        leia(r)
    fim
void converteDolar(real dolar, real totalReais)
    inicio
        dolar = 5.29
        totalReais = r
        totalGeral = totalReais/dolar
        escreva("A cotação é de: US$ " +totalGeral+)
    fim