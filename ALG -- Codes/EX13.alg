algoritmo EX13
    real area, base, altura
    início
        escreva("Digite a altura: ")
        leia(altura)

        escreva("Digite a base: ")
        leia(base)

        area = (base*altura)/2
        leia(area)
    fim