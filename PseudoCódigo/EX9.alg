algoritmo EX9
    inteiro a, b, total
    início
        escreva("Digite o primeiro valor: ")
        leia(a)
        escreva("Digite o segundo valor: ")
        leia(b)

        total = Math.floor(a/b)
        leia(total)
    fim