algoritmo EX8
    inteiro a, b, total
    início
        escreva("Digite as horas: ")
        leia(a)
        escreva("Digite os minutos: ")
        leia(b)

        total = b + (a*60)
        leia(total)
    fim